import React from 'react';
import ReactDOM from 'react-dom';
 // Optional: Include a CSS file for styling import './index.css';
import App from './App.jsx'; // Your main component

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
